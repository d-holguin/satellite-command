name: Deploy Satellite Command

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Refresh orbital elements and redeploy the static catalog every six hours.
    - cron: '17 */6 * * *'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure GitHub Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Refresh satellite data
        run: npm run data:satellites

      - name: Build Angular
        run: >
          npm run build --
          --configuration production
          --base-href="${{ steps.pages.outputs.base_path }}/"
          --output-path=dist/site

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist/site/browser

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
